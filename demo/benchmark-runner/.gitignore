node_modules
# Auto-generated by `npm run bench*`. Curated reports (committed manually
# with `git add -f path`) live alongside auto-generated runs; everything in
# this folder is gitignored by default so re-runs don't pollute the diff.
results/
